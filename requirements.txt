# Core lyrics chain (always installed; needed for both the ``lite`` and
# the ``portable`` PyInstaller builds).
mutagen>=1.47
requests>=2.31
lyricsgenius>=3.0

# The local audio-analysis fallback (faster-whisper + demucs + scipy +
# their transitive torch dependency) is a SEPARATE file --
# ``requirements-audio.txt``.  ``build-lite.bat`` does NOT install
# it, so the ``lite`` .exe stays at the documented ~50 MB footprint
# and does NOT support the local audio-analysis chain (LRCLIB + Genius
# only).  ``build-portable.bat`` does install it, so the ``portable``
# .exe bundles torch + demucs + faster-whisper + scipy in addition to
# the pre-seeded model weights and lands at ~3.5 GB on disk.
#
# See ``requirements-audio.txt`` for the audio-stack deps and
# ``README.md`` \u2192 "Building the executable" for the per-variant size
# table and the exact install command for dev runs that want
# Whisper + Demucs on the command line.
